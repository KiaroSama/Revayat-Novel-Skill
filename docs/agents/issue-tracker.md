# Issue Tracker

The user selected local Markdown records. Spec Kit owns feature specifications,
plans and task lists; use its selected feature rather than creating duplicate
tickets or publishing GitHub Issues. Preserve task IDs and completed evidence.
Consult the project's local planning configuration for the active feature.
